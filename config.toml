#general params
fractal_type = "grow"
iterations = 160000

#simple fractals
scaling = 0.25

#num_sys params
num_sys = 3

#line-based params
base_preset = "one line"
custom_lines = [
    [[300, 200], [-300, 200]],
    [[-300, 200], [0, -100]],
    [[0, -100], [300, 200]]
]

line_model_width = 0.5
line_model_height = 0.5
rotation_angle = 90

#spirals
spiral_angle = 90
spiral_scaling = 1.001