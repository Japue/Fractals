#general params
fractal_type = "pyth"
iterations = 16

#simple fractals
scaling = 0.25

#num_sys params
num_sys = 3

#line-based params
base_preset = "one line"
custom_lines = [
    [[300, 200], [-300, 200]],
    [[-300, 200], [0, -100]],
    [[0, -100], [300, 200]]
]

line_model_width = 0.8
line_model_height = 0.4
rotation_angle = 90

#spirals
spiral_angle = 1
spiral_scaling = 1.02